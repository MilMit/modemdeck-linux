[Unit]
Description=ModemDeck by MilMit lightweight cellular monitor
After=graphical-session.target

[Service]
Type=simple
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=/usr/bin/env modemdeck --monitor
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
